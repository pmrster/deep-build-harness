---
description: "Phase 7 — write README/CHANGELOG/API docs describing what was actually built"
---

Invoke the `harness-docs` skill and follow it exactly. Resolve the active run (session run-id, else `state/CURRENT`) and require every task in its `plans.json` to be "verified"; if not, tell me to run `/harness-work`. Document what exists in the real source, not the plan.

$ARGUMENTS
