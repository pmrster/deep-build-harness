---
description: "Phase 7 — write README/CHANGELOG/API docs describing what was actually built"
---

Invoke the `harness-docs` skill and follow it exactly. Require every task in `state/plans.json` to be "verified" first; if not, tell me to run `/harness-work`. Document what exists in the real source, not the plan.

$ARGUMENTS
