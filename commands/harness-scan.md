---
description: "Phase 1 — map the existing codebase read-only, then write state/codebase_map.md"
---

Invoke the `harness-scan` skill and follow it exactly. Require an approved `state/context.md` first; if it is missing, tell me to run `/harness-interview`. Map the codebase read-only and write `state/codebase_map.md`.

$ARGUMENTS
