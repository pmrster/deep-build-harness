---
description: "Phase 1 — map the existing codebase read-only, then write state/codebase_map.md"
---

Invoke the `harness-scan` skill and follow it exactly. Resolve the active run (session run-id, else `state/CURRENT`) and require its approved `context.md`; if missing, tell me to run `/harness-interview`. Map the codebase read-only and write the run's `codebase_map.md`.

$ARGUMENTS
