---
description: "Phase 0 — deeply interview requirements until unambiguous, then write state/context.md"
---

Invoke the `harness-interview` skill and follow it exactly. Interview me until the Must-Know Checklist is fully answered and confidence ≥ 80, then create the run directory `state/runs/<run-id>/`, write its `context.md`, record the run id in `state/CURRENT`, and ask for my approval before proceeding.

$ARGUMENTS
