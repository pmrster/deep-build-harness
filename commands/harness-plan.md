---
description: "Phase 3 — decompose architecture into parallel-wave tasks with machine-verifiable AC, write state/plans.json"
---

Invoke the `harness-plan` skill and follow it exactly. Require approved `state/context.md` and `state/architecture.md` first; if missing, tell me which phase to run. Write `state/plans.json` per `templates/plans.json.schema`, gate on my approval, then lock.

$ARGUMENTS
