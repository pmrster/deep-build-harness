---
description: "Verify harness environment and recover stuck states (stale active_role, missing dirs)"
---

Invoke the `harness-doctor` skill and follow it exactly. Run all environment checks in order, report results, and offer to clear a stale active_role if found.

$ARGUMENTS
