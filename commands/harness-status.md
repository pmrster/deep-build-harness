---
description: "Show the current harness run's phase progress and per-task status (read-only)"
---

Invoke the `harness-status` skill and follow it exactly. Resolve the active run (session run-id, else `state/CURRENT`) and report phase progress + a per-task status table for that run. Do not modify anything.

$ARGUMENTS
