---
description: "Fast path for a small change: lite scope, one confirm, then worker + independent auditor. /harness-quick [description]"
---

Invoke the `harness-quick` skill and follow it exactly. Do a lite scope pass on my described change, bail to `/harness-interview` if it turns out large, confirm once, then write a minimal locked 1-task run and drive the same harness-worker → harness-auditor loop with full accuracy guardrails and the independent audit gate.

$ARGUMENTS
