---
description: "Phase 2 — design schema, exact API contracts, file structure, then write state/architecture.md"
---

Invoke the `harness-architecture` skill and follow it exactly. Resolve the active run (session run-id, else `state/CURRENT`) and require its approved `context.md` and `codebase_map.md`; if missing, tell me which phase to run. Write the run's `architecture.md` and gate on my approval.

$ARGUMENTS
