---
description: "Phase 2 — design schema, exact API contracts, file structure, then write state/architecture.md"
---

Invoke the `harness-architecture` skill and follow it exactly. Require approved `state/context.md` and `state/codebase_map.md` first; if missing, tell me which phase to run. Write `state/architecture.md` and gate on my approval.

$ARGUMENTS
